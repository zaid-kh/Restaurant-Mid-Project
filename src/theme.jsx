import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    primary: {
      main: "#6f1a07",
    },
    secondary: {
      main: "#a8763e",
    },
    background: {
      default: "#281e16",
    },
    text: {
      primary: "#fdfefd",
    },
  },
});

export default theme;
